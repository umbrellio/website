{
  "name": "umbrellio-website",
  "private": true,
  "version": "UNVERSIONED",
  "license": "UNLICENSED",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "parcel build src/templates/*.pug",
    "start": "nodemon --watch src --ext js,scss,pug --exec bin/serve"
  },
  "devDependencies": {
    "http-server": "^0.11.1",
    "nodemon": "^1.19.1",
    "parcel-bundler": "^1.12.3",
    "pug": "^2.0.4",
    "rimraf": "^2.6.3",
    "sass": "^1.22.7"
  },
  "dependencies": {
    "jquery": "^3.4.1"
  }
}
